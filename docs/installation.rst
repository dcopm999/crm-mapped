============
Installation
============

At the command line::

    $ easy_install crm-mapped

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv crm-mapped
    $ pip install crm-mapped
